#include <bits/stdc++.h>

using namespace std;

int prev[300010][20], N, Q, p[300010];

int main(){
	freopen("test.txt","r",stdin);
	cin.sync_with_stdio(0); cin.tie(0);
	for (int i=0;i<20;++i)
		prev[0][i] = -1;

	cin >> N;
	for (int i=1; i<=N; ++i){
		int x; cin >> x;
		p[i] = i;
		prev[i][]
	}

	return 0;
}